let items = ["item1", "item2", "item3", "item4"]
items.unshift("item0") //add element in first position of the array
console.log(items);
items.shift() //renove element in first position of the array
console.log(items);
