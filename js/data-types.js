const message = "Hello"; // string
const number = 5; // number
const isstudent = true; //boolean
const colors = ["red", "blue", "greet"]; // array
const person = { name: "saimun", age: 19}; // object

console.log(typeof null); // "object" (Bug in JavaScript)
console.log(null === undefined); // false
console.log(null == undefined);  // true (Loose Comparison)
